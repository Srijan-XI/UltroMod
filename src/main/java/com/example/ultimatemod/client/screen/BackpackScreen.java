package com.example.ultimatemod.client.screen;

import net.fabricmc.api.EnvType;
import net.fabricmc.api.Environment;

/**
 * Placeholder — the backpack uses the vanilla GenericContainerScreen
 * via ScreenHandlerType.GENERIC_9X3 so no custom screen registration is needed.
 */
@Environment(EnvType.CLIENT)
public class BackpackScreen {
    // No-op
}
